/**
 * Config for the Chrome plugin. Code compiled here will be visible in the source so do not put anything sensitive in here.
 */
export const chromeConfig = {
  oauthToken: 'Generate your oauth token at https://console.cloud.google.com/iam-admin/serviceaccounts?project=your-project',
  slackChannelId: 'Bug Channel ID',
};
